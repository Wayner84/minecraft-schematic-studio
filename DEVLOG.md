# DEVLOG

## 2026-01-30
- Repo created and bootstrapped with Vite + React + TypeScript.
- Added PWA support for iOS Safari (Add to Home Screen) with standalone/full-screen display.
- Added placeholder icons + manifest.
- Added initial chunked world model scaffold (`src/app/model/world.ts`).

## Next
- Editor shell (layer slider + paint grid) with a swappable, data-driven block palette.
- 3D orbit viewer (Three.js) with shadow toggle.
- File save/load (local JSON) as the first sharing mechanism.
- Litematica (.litematic) import/export module (NBT + palette + packed blockstates).
- Redstone simulator MVP (tick stepping, pistons/doors) after core editing/viewing works.
